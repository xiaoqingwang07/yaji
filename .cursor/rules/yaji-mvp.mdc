---
description: 芽纪 MVP 全项目开发规则，规划、编码、测试或修改项目文件时必须遵守
globs:
alwaysApply: true
---

# 芽纪项目规则

开始任何开发工作前，完整阅读并执行：

@芽纪-MVP开发执行规格.md

`deep-research-report.md` 仅用于了解背景。若其内容与执行规格冲突，以执行规格为准，不得用研究稿中的远期设想扩大 MVP。

## 不可改变的产品边界

- 芽纪是家庭孕育档案工具，不是母婴社区、互联网医院或医疗诊断工具。
- MVP 必须覆盖孕期、妈妈产后恢复和宝宝 0-1 岁；宝宝成长、体检和疫苗记录属于核心范围。
- 时间轴是首页和数据主线。
- 健康模块是同一份时间轴数据的趋势视图，不得建立第二套重复数据或要求用户重复录入。
- OCR 是录入助手；结果必须经用户确认后才能进入正式档案。
- 所有合规医疗报告均允许上传和归档；未知类型必须退回通用 OCR 和手动补录，不能提示“不支持”。
- 不实现医疗诊断、风险预测、开放式健康问答、医生端、医院对接、社区或电商。
- 不实现喂奶、挤奶、睡眠、排泄、尿布次数等高频生活记录。
- 未经用户明确要求，不实现执行规格中的 P1 或“明确不做”功能。

## 执行要求

- 严格按执行规格第 12 节的四个项目大步骤推进。
- 当前目录没有代码时，先完成“整体设计与可复用原型”；原型必须建立在最终 Expo 工程中，不得创建后续丢弃的临时项目。
- 不要只生成静态 UI；移动端、API、数据库、对象存储和 Mock OCR 必须形成真实可运行闭环。
- 没有第三方密钥时使用 Mock Provider，项目仍须完整可演示。
- 普通实现细节按执行规格和成熟实践自行决策，不反复询问已有答案。
- 只有问题会改变产品边界、敏感数据处理方式或导致任务无法继续时，才向用户确认。
- 全量开发是一个连续大步骤，内部功能包只按依赖排序，不重复设计、不创建多套版本，也不在每个功能包之间停下来重新规划。
- 功能开发时同步编写测试；每个大步骤结束后运行相应的 lint、类型检查、测试和构建，只汇报实际运行结果。
- 同步维护 README、`.env.example`、数据库迁移、脱敏种子数据和 OpenAPI 文档。
- 不在代码、日志、测试夹具或文档中写入真实手机号、产检报告或服务密钥。

## 首次执行顺序

1. 创建 `PROJECT_PLAN.md` 和需求追踪矩阵，覆盖执行规格的全部 MVP 要求。
2. 创建规定的 TypeScript monorepo，并在最终 Expo 工程中完成可复用交互原型。
3. 同步确定 Prisma Schema、OpenAPI、共享契约、权限和测试设计。
4. 完成唯一一次产品原型确认后，连续完成全部 MVP 开发。
5. 统一执行系统测试、集中修复、发布验收和交付。
6. 每个项目大步骤按执行规格第 16 节的格式汇报。
